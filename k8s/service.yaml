apiVersion: v1
kind: Service
metadata:
  name: dentalclinic-backend-svc
  namespace: k8s-duha
spec:
  type: LoadBalancer
  ports:
    - port: 5001
      targetPort: 5001
  selector:
    app: dentalclinic